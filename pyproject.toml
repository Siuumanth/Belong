[tool.pyright]
executionEnvironments = [
  { root = "belong-workers", extraPaths = ["belong-workers"], venvPath = "belong-workers", venv = ".venv" },
  { root = "belong-api", extraPaths = ["belong-api"], venvPath = "belong-api", venv = ".venv" }
]
