[pytest]
asyncio_mode = auto
markers =
    live: Tests invoking real external LLM API calls (Groq)
    e2e: Integration tests requiring running microservices (Postgres, RabbitMQ, belong-api, belong-workers)
    simulation: Realistic user persona simulation tests
pythonpath = . belong-api belong-workers tests

